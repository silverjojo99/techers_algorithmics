<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.algorithmics">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Projet_Stage_Algorithmics"
        tools:targetApi="31">

        <!-- Login (Launcher Activity) -->
        <activity
            android:name="com.example.algorithmics.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dashboard (with Drawer) -->
        <activity
            android:name="com.example.algorithmics.MainDashboardActivity"
            android:exported="false" />

        <!-- Course list -->
        <activity
            android:name="com.example.algorithmics.CourseListActivity"
            android:exported="false" />

        <!-- Trends -->
        <activity
            android:name="com.example.algorithmics.TrendsActivity"
            android:exported="false" />

        <!-- Projects -->
        <activity
            android:name="com.example.algorithmics.ProjectsActivity"
            android:exported="false" />

    </application>

</manifest>